@model Nop.Web.Areas.Admin.Models.Blogs.Categories.BlogCategoryModel
@{
    //page title
    ViewBag.PageTitle = T("Admin.ContentManagement.BlogCategories.AddNew").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("BlogCategories");
}

<form asp-controller="BlogCategory" asp-action="Create" method="post" id="blog-category-form">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.ContentManagement.BlogCategories.AddNew")
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.ContentManagement.BlogCategories.BackToList")</a>
            </small>
        </h1>
        <div class="float-right">
            <button type="submit" name="save" class="btn btn-primary">
                <i class="far fa-floppy-disk"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn btn-primary">
                <i class="far fa-floppy-disk"></i>
                @T("Admin.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync(typeof(AdminWidgetViewComponent), new { widgetZone = AdminWidgetZones.BlogCategoryDetailsButtons, additionalData = Model })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>